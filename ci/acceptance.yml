---
platform: linux

image_resource:
  type: docker-image
  source: { repository: golang }

inputs:
  - name: silk
    path: go/src/github.com/cloudfoundry-incubator/silk

run:
  path: /bin/bash
  args:
    - -c
    - |
      GOPATH=${PWD}/go && \
      $GOPATH/src/github.com/cloudfoundry-incubator/silk/scripts/test.sh
